version: '3.8'

services:
  psychobot:
    build: .
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/psychobot.db
    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db
    restart: unless-stopped
